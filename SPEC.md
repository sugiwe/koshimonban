# Tatsumaki — ストレッチ強制 macOS アプリ 実装仕様書

アプリ名: `Tatsumaki`（竜巻） / Bundle Identifier: `net.sugiwe.tatsumaki`

---

## 0. 背景とゴール

作者は座りっぱなしのデスクワークで腰を痛めた。30分に1回立ち上がるのが理想だが、意志では続かない。
そこで **意志に頼らず、決まった時刻に画面を物理的に奪って立ち上がらせる** アプリを作る。

このアプリの本質は機能の数ではなく **「無視されにくさ」** にある。
実装判断で迷ったら、次の優先順位で決めること。

1. 発動が確実であること（作業時間帯なら、何があっても画面が出る）
2. 逃げ道はあるが、摩擦があること（スキップは押せるが、無自覚には押せない）
3. サボった事実が残ること
4. 見た目や機能の豊かさ（最下位）

### 非ゴール（v1では作らない）

- Google カレンダー連携
- 複数ユーザー・アカウント・同期
- App Store 配布（ローカルビルドで自分だけが使えれば良い）
- iOS 版

---

## 1. 技術スタックと前提

| 項目 | 決定 |
| --- | --- |
| 言語 / UI | Swift + SwiftUI（オーバーレイなど低レベルなウィンドウ制御は AppKit を直接使う） |
| 最小 macOS | macOS 14 以上（開発機は macOS 15.6.1） |
| Xcode | 16 以上 |
| アプリ形態 | メニューバー常駐アプリ（Dock にアイコンを出さない = `LSUIElement = true`） |
| 外部依存 | なし（標準フレームワークのみ） |
| データ保存 | JSON ファイル（DB は使わない） |

### 選定理由（変更を検討する際の判断材料）

- **常駐アプリなのでメモリが軽いこと**を優先。Electron は数百MB常駐するため見送った。
- **最前面への割り込み**（フルスクリーンアプリの上、全 Space に表示）が機能の中核であり、
  ここは OS のウィンドウレベルを直接扱える方が安定する。
- 将来の通話中検出（CoreAudio）や App Store 配布への道を塞がない。

### Claude Code への重要な注意

- **`.xcodeproj` / `project.pbxproj` を手で編集しないこと。** 高確率で壊れる。
  最初の空プロジェクトは人間が Xcode の「macOS App」テンプレートから1回だけ作成する。
- ソースは `Tatsumaki/Sources/` 配下に置き、Xcode 側では**フォルダ参照（buildable folder）**として
  追加しておく。こうしておけばファイルを増やしても Xcode プロジェクトの編集が不要になる。
- ビルド確認は CLI から行うこと:
  ```
  xcodebuild -project Tatsumaki.xcodeproj -scheme Tatsumaki -configuration Debug build
  ```
- 実行時の挙動（オーバーレイが本当に最前面に出るか等）は人間の目視確認が必要。
  各フェーズの末尾に「人間が確認すること」を明記してあるので、そこで一旦止めて報告すること。

---

## 2. 用語

| 用語 | 意味 |
| --- | --- |
| 作業時間帯（Work Block） | ストレッチを発動させたい時間の範囲。例: 10:00–12:00, 14:00–17:00 |
| 間隔（Interval） | 発動の間隔。分単位。例: 30分 |
| 休憩（Break） | 1回の発動。既定 3分間 |
| オーバーレイ | 休憩中に全画面を覆うウィンドウ |
| スキップ | 休憩を中断して作業に戻ること。記録に残る |
| 一時停止（Snooze） | 一定時間、発動そのものを止めること |

---

## 3. 機能要件

### 3.1 設定

設定画面（メニューバーから開く通常ウィンドウ）で以下を編集できる。

**作業時間帯**

- 複数登録できる（開始時刻・終了時刻のペアのリスト）
- 曜日ごとの有効/無効を持つ（既定: 月〜金が有効、土日は無効）
- 時間帯が重なっている場合はマージして扱う。**マージは同じ曜日どうしでのみ行う**
- 追加・削除・編集ができること

**タイミング**

- 間隔: 分単位（既定 30分）
- 休憩の長さ: 秒単位（既定 180秒 = 3分）
- 予告通知: 発動の何分前に通知するか（既定 1分前。0 で無効）

**動画**

- 動画エントリのリスト。1件は次を持つ:
  - タイトル（任意）
  - 種別: `youtube` または `local`
  - YouTube の URL、またはローカル動画ファイルのパス
- 0件でも動作すること（その場合はテキストとカウントダウンのみ表示）

**その他**

- ログイン時に自動起動する（既定 ON。**LaunchAgent 方式**、3.6 参照）
- デバッグモード（後述）

### 3.2 スケジューラ

**発動時刻の決め方**

発動時刻は **各作業時間帯の開始時刻を起点とした固定グリッド** で決める。

```
例: 作業時間帯 10:00–12:00、間隔 30分
→ 発動時刻は 10:30, 11:00, 11:30 （12:00 は時間帯の終端なので発動しない）
```

- 「前回の休憩終了時刻から N分後」にはしない。ズレが蓄積して発動時刻が予測できなくなり、
  体がリズムを覚えられなくなるため。
- スキップしてもグリッドはズレない。次は元のグリッド通りに来る。
- **重なる時間帯をマージした場合、グリッドの起点はマージ後の開始時刻とする。**
  例: 10:00–12:00 と 11:00–13:00 → 10:00–13:00 とみなし、起点は 10:00（10:30, 11:00, 11:30 …）

**境界条件（必ず実装すること）**

| 状況 | 挙動 |
| --- | --- |
| 発動時刻から休憩終了までが作業時間帯をはみ出す | 発動しない（例: 3分休憩なら 11:58 に発動しない） |
| Mac がスリープしていて発動時刻を過ぎた | 復帰時、まだ作業時間帯内なら **30秒後** に発動する。過ぎていれば次のグリッドを待つ |
| 画面がロックされている | 発動しない。ロック解除後、作業時間帯内なら 30秒後に発動 |
| 発動時刻に既にオーバーレイが出ている | 二重に出さない（無視する） |
| 一時停止中に発動時刻が来た | 発動しない。記録には `paused` として残す |
| スリープ/ロックのまま作業時間帯が終わった | 発動できなかった回として `missed` を記録する |
| 日付をまたぐ作業時間帯（例 23:00–02:00） | v1では非対応。設定画面でバリデーションエラーにする |

**実装上の注意**

- `Timer` を発動時刻ぴったりに1本仕掛ける方式は、スリープや時刻変更で簡単に壊れる。
  **30秒ごとに起きて「今、発動すべきか」を判定する**ループにすること。
- スリープ復帰は `NSWorkspace.didWakeNotification` で検知。
- 画面ロック状態は `DistributedNotificationCenter` の `com.apple.screenIsLocked` /
  `com.apple.screenIsUnlocked` を主に使い、`CGSessionCopyCurrentDictionary` の
  `kCGSSessionOnConsoleKey` を併用して冗長化する。
- 設定ウィンドウを開いている最中でも、発動時刻が来ればオーバーレイは出す。

### 3.3 オーバーレイ（このアプリの中核）

> **スパイク検証済み（macOS 15.6.1 / 2026-08-27）。** `spike/` の検証アプリで以下を確認した。
> この節の方針は確定であり、実装時に迷ったらスパイクの `spike/main.swift` を参照すること。
>
> | 確認項目 | 結果 |
> | --- | --- |
> | 他アプリのネイティブフルスクリーンの上に出るか | 出る |
> | Space（デスクトップ）が勝手に切り替わらないか | 切り替わらない。閉じると元のフルスクリーン作業がそのまま戻る |
> | 全ディスプレイを覆えるか | 覆える |
> | キーボードフォーカスを奪えるか | 奪える（`isKeyWindow` = YES、打鍵がオーバーレイに届く） |

**表示**

- **全ディスプレイ**に表示する。外部ディスプレイ使用時にサブ画面だけ作業が続けられると意味がない。
  - メインディスプレイ: 動画・カウントダウン・ボタン
  - サブディスプレイ: 暗転 + カウントダウンのみ
- ウィンドウレベルは `CGShieldingWindowLevel()` 相当（スクリーンセーバーより上）。
  フルスクリーンで動画やエディタを開いていても上に出ること。
- `collectionBehavior` は `[.canJoinAllSpaces, .fullScreenAuxiliary]`（検証済み。これで Space は切り替わらない）。
- 表示時にフォーカスを奪う。`NSApp.activate(ignoringOtherApps:)` は macOS 14 で deprecated のため
  `NSApp.activate()` + `NSRunningApplication.current.activate(options:)` を使う。
- borderless ウィンドウは既定で key window になれないため、`NSWindow` を継承して
  `canBecomeKey` / `canBecomeMain` を `true` に上書きすること。**ここを忘れると
  「画面は覆えているのにタイプは下のアプリに通る」状態になる。**
- 背景は不透明（半透明にして下の画面が見えると作業を続けようとするため）。
- **休憩中のディスプレイ抜き差しに対応する。** `NSApplication.didChangeScreenParametersNotification`
  を購読し、オーバーレイのウィンドウ構成を作り直す。

**入力の封じ込め範囲（v1の決定）**

- オーバーレイを key window にしてキー入力を受け取るところまでとする。
- **Cmd-Tab や Cmd-Q の封じ込め（`NSApp.presentationOptions` の `.disableProcessSwitching` 等）は
  v1 では行わない。** アクセシビリティ機能との干渉や、アプリが固まった際に抜けられなくなる
  リスクのほうが大きいと判断した。実際に使ってみて「簡単に抜けられてしまう」と感じたら再検討する。

**中身**

- 大きなカウントダウン（残り 2:59 …）
- 動画（登録があれば自動再生）
- ボタン2つ: 「終わった」「スキップ」

**スキップの摩擦設計（重要）**

- **スキップボタンは表示から 5秒間は押せない**（無効化 + 「5」からのカウントダウン表示）。
  反射でスキップを押す癖がつくのを防ぐため。
- スキップを押すと、理由を3択で選ばせる: `MTG直前` / `集中してる` / `気分じゃない`
  - 選択せず閉じることもできる（その場合は理由なしで記録）
- Esc キーではオーバーレイを閉じない。
- 「終わった」はカウントダウン中でも押せる（早く終わった場合のため）。

**完了判定**

| 操作 | 結果 |
| --- | --- |
| カウントダウンが 0 になった | `completed` |
| 「終わった」を押した（残り 30秒未満） | `completed` |
| 「終わった」を押した（残り 30秒以上） | `short`（短縮） |
| 「スキップ」を押した | `skipped` |

**逃げ道（必須）**

メニューバーから以下を実行できること。これがないと、本当に困った時にアプリごと終了され、
二度と起動されなくなる。

- 「30分停止」「1時間停止」「今日はもう停止」
- 一時停止中はメニューバーアイコンで分かるようにする（アイコンを薄くする等）

### 3.4 動画再生

- 動画が複数登録されている場合、**発動ごとにローテーション**する（前回と同じものを続けて出さない）。
- **YouTube: 単純な embed URL の読み込みではなく、IFrame Player API を使う。**
  - 理由: 埋め込み禁止動画・地域制限・autoplay ブロックはいずれも「WKWebView のロード自体は成功」
    してしまうため、単純な埋め込みでは失敗を検出できない。**動画が黙って死んでいるのに
    気付かない**状態を避ける。
  - ローカル HTML に IFrame Player API を埋め、`onError` / `onStateChange` を
    `WKScriptMessageHandler` 経由で Swift に返す。
  - **5秒以内に `PLAYING` にならなければフォールバック**（テキスト + カウントダウンのみの表示）。
  - URL から video ID を抽出し、`youtube-nocookie.com` のホストを使う。
  - 自動再生のため `WKWebViewConfiguration.mediaTypesRequiringUserActionForPlayback = []` を設定。
    それでも弾かれる場合に備え、muted で開始して即 unmute する回避策を用意する。
- ローカル: `AVPlayer` で再生。ファイルが見つからない場合も同様にフォールバック。
- 音量はシステム音量に従う。アプリ側で勝手に上げない。

### 3.5 記録

**記録する内容**（1回の発動ごと）

- 発動予定時刻
- 実際の発動時刻
- 結果: `completed` / `skipped` / `short` / `missed` / `paused`
- スキップ理由（あれば）
- 実際に画面が出ていた秒数
- 再生した動画のタイトル

**見せ方**

- メニューバーのタイトルに当日の達成状況を出す: `3/5`
- 設定ウィンドウ内の「記録」タブに:
  - 当日の一覧（時刻と結果）
  - 直近28日間のカレンダー状ヒートマップ（達成率で濃さが変わる）
  - 直近7日 / 30日の達成率
- **スキップ回数とスキップ理由の内訳を必ず表示すること。** 記録機能の目的は
  達成を褒めることではなく、サボりのパターンを可視化することにある。

### 3.6 ログイン時の自動起動（LaunchAgent 方式）

`SMAppService.mainApp` ではなく、**`~/Library/LaunchAgents/net.sugiwe.tatsumaki.plist` を
アプリ自身が書き出す方式**を採る。

**理由**: `SMAppService` は正しいコード署名を要求する。Apple Developer Program に入らない
ローカルビルド運用では、無料 Apple ID の Development 証明書が7日で失効し、アプリが起動しなくなる。
毎週ビルドし直す運用は「意志に頼らない」というコンセプトと正面から衝突する。

**副次的な利点**: plist に `KeepAlive` を設定することで、**クラッシュしても自動で復帰する。**
「アプリを殺されたら終わり」というこのアプリ固有の弱点への保険になる。

設定の ON/OFF に応じて plist を書き出す / 削除し、`launchctl bootstrap` / `bootout` を呼ぶ。

### 3.7 予告通知

`UserNotifications` ではなく、**自前の小さなフローティングウィンドウ**を発動の N分前に出す。

**理由**: 通知センターの通知は署名済みバンドルを前提とし、ローカルビルドでは権限ダイアログが
出ないことがある。加えて通知は無視されやすく、このアプリの趣旨に合わない。自前ウィンドウなら
権限リクエストも不要になる。

---

## 4. データ

保存先: `~/Library/Application Support/Tatsumaki/`

```
settings.json          設定
logs/2026-08.json      月ごとの記録
```

SQLite を使わない理由: データ量が小さく、人間が直接開いて直せる方が事故に強いため。

### settings.json

```json
{
  "version": 1,
  "workBlocks": [
    { "id": "uuid", "start": "10:00", "end": "12:00", "weekdays": [1,2,3,4,5] },
    { "id": "uuid", "start": "14:00", "end": "17:00", "weekdays": [1,2,3,4,5] }
  ],
  "intervalMinutes": 30,
  "breakSeconds": 180,
  "preNotifyMinutes": 1,
  "skipUnlockSeconds": 5,
  "videos": [
    { "id": "uuid", "title": "肩と腰のストレッチ", "kind": "youtube", "url": "https://www.youtube.com/watch?v=xxxx" },
    { "id": "uuid", "title": "朝用", "kind": "local", "path": "/Users/xxx/Movies/stretch.mp4" }
  ],
  "launchAtLogin": true,
  "debugMode": false
}
```

`weekdays` は 1=月曜 … 7=日曜。

### logs/YYYY-MM.json

```json
{
  "2026-08-27": [
    {
      "scheduledAt": "10:30",
      "firedAt": "10:30",
      "result": "completed",
      "skipReason": null,
      "shownSeconds": 180,
      "videoTitle": "肩と腰のストレッチ"
    },
    {
      "scheduledAt": "11:00",
      "firedAt": "11:00",
      "result": "skipped",
      "skipReason": "meeting",
      "shownSeconds": 7,
      "videoTitle": "肩と腰のストレッチ"
    }
  ]
}
```

- 書き込みは1回の発動が終わるたび（アプリが落ちても記録が消えないように）
- ファイルが壊れていた場合は読み飛ばして新規作成し、壊れたファイルは `.corrupt` にリネームして残す

---

## 5. デバッグモード（最初に実装すること）

**このアプリは実時間で動くため、そのままでは1回のテストに30分かかる。**
開発の最初の段階で必ず以下を入れること。

- `debugMode` が true のとき:
  - 間隔の単位を「分」から「秒」として扱う（30 → 30秒）
  - 休憩の長さも短縮できる（10秒など）
  - 作業時間帯の判定を無視して常に作業中とみなすオプション
- メニューバーに「今すぐ発動」を常時置く（デバッグモードでなくても便利）
- 記録のダミーデータを生成するコマンド（ヒートマップの見た目確認用）

---

## 6. 実装フェーズ

各フェーズの終わりで一度止めて、人間の確認を受けること。

### Phase -1: スパイク検証 — 完了（2026-08-27）

`spike/` の使い捨てアプリで設計の前提を検証し、**4項目すべて通過した。**
結果は 3.3 の表を参照。`spike/` は実装中の参照用に残すが、本体には含めない。

### Phase 0: 土台

- 人間が Xcode で空の macOS App プロジェクトを作成（ここだけ手動）
- メニューバー常駐（`MenuBarExtra`）、Dock に出さない設定
  - `MenuBarExtra` は `3/5` の動的更新やアイコンの減光にクセがあるため、
    詰まった場合は `NSStatusItem` に切り替える
- 設定の読み書き（settings.json）
- メニューバーに「設定を開く」「今すぐ発動」「終了」

**人間が確認**: メニューバーにアイコンが出る / Dock に出ない / 設定ファイルが生成される

### Phase 1: 発動の仕組み（ここが一番大事）

- スケジューラ（30秒ループ、固定グリッド、境界条件）
- 設定画面の作業時間帯・間隔の編集 UI
- オーバーレイ（テキスト + カウントダウン + ボタン2つ、動画なし）
- 全ディスプレイ表示、最前面、フォーカス奪取、ディスプレイ抜き差し対応
- スキップの5秒ロックと理由選択
- デバッグモード

**人間が確認**: フルスクリーンのアプリを開いた状態で、その上にオーバーレイが出るか。
外部ディスプレイ側も覆われるか。スリープから復帰した時の挙動。

### Phase 2: 動画

- 動画エントリの登録 UI
- YouTube IFrame Player API 埋め込み / ローカル再生 / ローテーション / フォールバック

**人間が確認**: 自動再生されるか。ネットを切った状態でもオーバーレイ自体は正常に動くか。
埋め込み禁止の動画を登録した場合にフォールバックが効くか。

### Phase 3: 記録

- 記録の保存
- メニューバーの `3/5` 表示
- 記録タブ（当日一覧・ヒートマップ・達成率・スキップ理由の内訳）

### Phase 4: 仕上げ — 完了

- ログイン時自動起動（LaunchAgent の書き出しと `launchctl bootstrap`）
- 多重起動の防止。LaunchAgent 登録直後は launchd がもう1つ起動するため、後発が引き下がる
- 予告（自前フローティングウィンドウ。フォーカスは奪わず、クリックも透過させる）
- 一時停止メニュー（30分 / 1時間 / 今日）
- アイコン（`tools/make-icon.sh` で生成）

---

## 7. v2 以降の候補（実装しないが、設計を塞がないこと）

- **通話中の自動延期**: CoreAudio の `kAudioDevicePropertyDeviceIsRunningSomewhere` で
  マイク使用中を検知し、通話が終わるまで発動を遅らせる。Google カレンダー連携より軽く、
  実用上の効果は大きい可能性が高い。
- Google カレンダー連携（MTG 以外の予定を作業時間帯として自動検出）
- 休憩中のキーボード/マウス操作を検知して「座ったまま流した」を記録に残す
- 週次サマリの通知
- Cmd-Tab の封じ込め（v1 では見送った。使ってみて必要性を判断する）
- App Store 配布（Apple Developer Program が年99ドル必要）

---

## 8. 既定値（使ってみて違和感があれば変更する）

- スキップボタンのロック時間 5秒
- 予告 1分前
- 発動グリッドは作業時間帯の開始起点（10:00 開始なら 10:30, 11:00 …）
- 作業時間帯の終端では発動しない
- 休憩の長さ 3分
- 「終わった」を残り30秒以上残して押したら `short` 扱い
